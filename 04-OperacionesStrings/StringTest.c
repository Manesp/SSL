#include <stdio.h>
#include "string.h"
#include <assert.h>

int void main(){
    testGetLength();
    testPower();
    testIsEmpty();
}

void testGetLength(){
    assert(1 == GetLength("a"));
    assert(0 == GetLength(""));
    assert(5 == GetLength("ababa"));
}

void testPower(){
    assert("abab" == )
}